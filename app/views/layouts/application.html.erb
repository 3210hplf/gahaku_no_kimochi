<!DOCTYPE html>
<html>
  <head>
    <title>GahakuNoKimochi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <!-- twitterカード -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="@runte999">
    <meta name="twitter:title" content="がはくのきもち">
    <meta name="twitter:description" content="わすれかけたこどもごころ。かんせいをよびさまそう。">
    <meta name="twitter:image" content="https://gahaku-no-kimochi.onrender.com/assets/gahaku_no_kimochi.png">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if logged_in? %>
      <%= render 'shared/header' %>
    <% else %>
      <%= render 'shared/before_login_header' %>
    <% end %>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
    <%= render 'shared/footer' %>
  </body>
</html>
